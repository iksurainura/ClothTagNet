1) first clone the project using # Clone the repository
git clone https://github.com/iksurainura/ClothTagNet.git

# Create Virtual enviroment 
python -m venv venv
venv/Scripts/activate  

# Install dependencies
npm install
pip install -r requirements.txt
